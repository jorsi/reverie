let socket;

export function init(socket) {
    // register receiving events
    socket = socket;

    // receiving events
    // socket.on('connect', () => events.emit('server/connected', this));
    // socket.on('player/init', (e) => events.emit('player/init', e));
    // socket.on('player/update', (playerEntity) => events.emit('player/update', playerEntity));
    // socket.on('world/init', (world) => events.emit('world/init', world));
    // socket.on('world/world', (wm) => events.emit('world/world', wm));
    // socket.on('world/update', (world) => events.emit('world/update', world));
    // socket.on('debug/maps', (maps) => events.emit('debug/maps', maps));
}